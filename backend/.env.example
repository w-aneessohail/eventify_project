# Server
PORT=5002

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=Assa@1234
DB_NAME=eventify_db

# Auth
JWT_SECRET=change_me_to_a_long_random_string
SALT_ROUNDS=10

# Email (OTP) — e.g. Mailtrap for local dev
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USER=your_mail_user
MAIL_PASS=your_mail_pass
MAIL_FROM="Eventify <no-reply@eventify.test>"
